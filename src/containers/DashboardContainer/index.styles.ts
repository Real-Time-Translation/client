import { createUseStyles } from 'react-jss';

export const useStyles = createUseStyles({
  root: {
    marginTop: 8,
  },
  roomsWrapper: {
    backgroundColor: '#323940',
    borderRadius: 12,
    boxShadow: 12,
    padding: 18,
    marginTop: 24,
  },
});
