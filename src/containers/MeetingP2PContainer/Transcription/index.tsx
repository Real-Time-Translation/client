import React, { FC } from 'react';
import { useStyles } from './index.styles';

export const Transcription: FC = () => {
  const classes = useStyles();
  return <section className={classes.root}></section>;
};
